# Yandex.Content

 ласс дл€ организации автоматической загрузки новостей в яндекс.¬ебмастер

###  ласс YandexContent предоставл€ет простой API:

ѕостинг текста на €ндекс и св€зь его с заданным сайтом:

```php
postArticle($text, $site_name)
```

¬ернуть список ваших сайтов. в случае, если необ€зательный параметр равен true, возвращаютс€ только доступные дл€ постинга текстов сайты

```php
getSiteList($only_approved = true)
```

–абота строитс€ из следующих телодвижений:

 - создать объект YandexContent, передав в конструктор ваш логин и пароль на €ндексе. Ќеоб€зательный третий параметр указывает, нужно ли использовать посто€нную сессию (true) или нужно очистить сессию по окончании работы (false):

```php
$YC = new YandexContent('login', 'passwd', true);
```

 - запостить текст

```php
$YC->postArticle($text, $site_name);
```

“еперь, если в браузере пройти в интерфейс и открыть яндекс.¬ебмастер - <»м€ сайта> - ќригинальные тексты, то веро€тно можно увидеть добавленные через класс тексты
при необходимости, можно использовать методы:

```php
setInputEncoding($enc)          // установить кодировку с вашей стороны (если автоопределение работает неверно)
setCurlCookieFilename($file)    // установить путь к curl-cookie-файлу (иначе делаетс€ автоматически)
isLoggedIn()                    // возвращает true, если авторизаци€ на €ндексе актуальна
getArticleList($site_name)      // возвращает список запощенных текстов дл€ данного сайта
getError(),getErrno()           // доступ к ошибкам
```
